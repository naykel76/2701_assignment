import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '2701_assignment',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
